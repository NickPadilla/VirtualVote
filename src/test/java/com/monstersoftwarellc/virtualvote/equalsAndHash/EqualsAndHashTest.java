/**
 * 
 */
package com.monstersoftwarellc.virtualvote.equalsAndHash;

import com.monstersoftwarellc.virtualvote.IModel;

/**
 * Test all my IModel classes so that we can ensure we have valid equals
 * and hash methods.  Use reflection to test ALL {@link IModel} classes,
 * hopefully i can get all concrete implementations of {@link IModel}'s and 
 * create a new object then fill it up with data, if a field is an object we create
 * a new one of those and fill it up.  This way we can ensure we don't have any
 * problems here.
 * @author nicholas
 *
 */
public class EqualsAndHashTest {

}
